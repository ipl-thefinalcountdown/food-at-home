{
  "private": true,
  "name": "websockets",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "nodemon src/index.ts",
    "start": "npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nodemon": "^2.0.6",
    "socket.io": "^2.3.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.12",
    "@types/socket.io": "^2.1.12"
  }
}
